import { BestquoteDirective } from './bestquote.directive';

describe('BestquoteDirective', () => {
  it('should create an instance', () => {
    const directive = new BestquoteDirective();
    expect(directive).toBeTruthy();
  });
});
